# LogGrid

This project contains a backend API built with C# and a frontend application.

## Backend

The `Backend/` directory contains the C# ASP.NET Core Web API for the Intern Attendance System.

## Frontend

The `Frontend/` directory contains the client-side application.
